version: 0.0
os: linux 
files:
  - source: /
    destination: /var/www/html 
hooks:
  AfterInstall:
    - location: script/install.sh
      timeout: 300 
      runas: root
  ApplicationStart:
    - location: script/run.sh 
      timeout: 300
      runas: root